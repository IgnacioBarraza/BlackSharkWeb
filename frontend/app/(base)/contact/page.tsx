import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contacto | BlackSharkStudios",
  description: "¡Ponte en contacto con BlackSharkStudios!"
}

const page = () => {
  return (
    <div>Ésta será la página de contacto!</div>
  )
}

export default page